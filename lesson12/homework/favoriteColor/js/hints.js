//These are your hints, don't type in this file, use the fav_color.js instead. 

$(

  function(event) {

  	console.log("Prompt the user with prompt() function.");
	console.log("Store the favorite color in a variable");
	console.log("Use the || in your conditional statement");
	console.log("functions .html() and .css() (see class one) changes the index.html file.");
  }
);